<%= render 'shared/alerti' %>
<div class="row">
<%= render 'shared/left_sidebar_admin' %>
<div class="col-lg-9">
<%- model_class = Raspisivac -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human %></h1>
</div>

<dl class="dl-horizontal">
</dl>
<%= image_tag @raspisivac.avatar.url(:thumb) %>
<h3><%= @raspisivac.name %></h3>
<div class="form-actions">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              raspisivacs_path, :class => 'btn'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_raspisivac_path(@raspisivac), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              raspisivac_path(@raspisivac),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>
