<div class="row">
<%= render 'shared/left_sidebar_admin' %>
<div class="col-lg-9">
<%- model_class = Raspisivac -%>
<div class="page-header">
  <h1>Raspisivači</h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @raspisivacs.each do |raspisivac| %>
      <tr>
        <td><%= link_to raspisivac.id, raspisivac_path(raspisivac) %></td>
        <td><%=l raspisivac.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_raspisivac_path(raspisivac), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      raspisivac_path(raspisivac),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_raspisivac_path,
            :class => 'btn btn-primary' %>
</div>
</div>